# 动作识别规则报告

本文档整理当前 motion_capture 项目中新增的规则式动作识别功能。内容分为两部分：第一部分给出最终版动作判断标准与 confidence 计算方式；第二部分记录调试过程中的规则调整，作为探索过程说明。

## 1. 设计目标

本次完善的动作识别功能用于识别 6 类静态上肢动作：

| 动作名称 | 对应测试视频 | 含义 |
|---|---|---|
| `V_bottom` | `tests/V_bottom.mp4` | 下 V |
| `T_big` | `tests/T_big.mp4` | 大 T |
| `V_up` | `tests/V_up.mp4` | 上 V |
| `H_up` | `tests/H_up.mp4` | 上 H |
| `K_r` | `tests/K_r.mp4` | 右 K |
| `K_l` | `tests/K_l.mp4` | 左 K |

这些动作不依赖 DTW 模板，而是根据 MediaPipe Pose 输出的关键点坐标进行几何规则判断。

## 2. 使用的关键点

| 编号 | 关键点 |
|---:|---|
| 11 | 左肩 |
| 12 | 右肩 |
| 13 | 左肘 |
| 14 | 右肘 |
| 15 | 左手腕 |
| 16 | 右手腕 |
| 23 | 左髋 |
| 24 | 右髋 |

## 3. V / T / H 动作判断标准

### 3.1 基础角度定义

左侧肩部角度定义为：

```text
angle_left = angle(13, 11, 23)
```

也就是左臂线段 `(11,13)` 与左躯干线段 `(11,23)` 的夹角。

右侧肩部角度定义为：

```text
angle_right = angle(14, 12, 24)
```

也就是右臂线段 `(12,14)` 与右躯干线段 `(12,24)` 的夹角。

### 3.2 最终判断区间

| 动作 | 判断条件 |
|---|---|
| 下 V `V_bottom` | `30° <= angle_left <= 60°`，且 `30° <= angle_right <= 60°` |
| 大 T `T_big` | `80° <= angle_left <= 100°`，且 `80° <= angle_right <= 100°` |
| 上 V `V_up` | `110° <= angle_left <= 165°`，且 `110° <= angle_right <= 165°` |
| 上 H `H_up` | `160° <= angle_left <= 180°`，且 `160° <= angle_right <= 180°` |

说明：`V_up` 与 `H_up` 在 `160°-165°` 之间存在重叠区间，因此某些帧可能同时命中 `V_up` 和 `H_up`。这是为了降低 `V_up` 漏检率而保留的容错设计。

## 4. V / T / H Confidence 计算

对 V / T / H 类动作，confidence 由两部分组成：

1. 左右肩部角度是否接近目标范围中心。
2. 左右两侧动作是否对称。

### 4.1 范围得分

对于目标范围 `[lo, hi]`：

```text
center = (lo + hi) / 2
half_width = (hi - lo) / 2
range_score(x) = clamp(1 - abs(x - center) / half_width, 0, 1)
```

其中 `x` 是当前角度。角度越接近区间中心，得分越高；越靠近边界，得分越低。

左右角度综合得分为：

```text
angle_score = (range_score(angle_left) + range_score(angle_right)) / 2
```

### 4.2 对称性得分

```text
symmetry_score = clamp(1 - abs(angle_left - angle_right) / 30, 0, 1)
```

左右角度差越小，对称性得分越高。

### 4.3 最终 confidence

```text
confidence = 0.8 * angle_score + 0.2 * symmetry_score
```

因此，V / T / H 的百分比主要反映动作角度是否接近标准姿态，同时少量考虑左右对称性。

## 5. K 动作判断标准

K 动作的判断经过多轮调整，最终采用“上臂夹角 + 肩腕线段相对躯干位置 + 身体中心侧向约束”的组合规则。

### 5.1 上臂夹角

左上臂向量：

```text
left_upper_arm = 13 - 11
```

右上臂向量：

```text
right_upper_arm = 14 - 12
```

计算两条上臂向量的有符号夹角，并取绝对值：

```text
arm_angle = abs(signed_angle(left_upper_arm, right_upper_arm))
```

最终 K 动作要求：

```text
65° <= arm_angle <= 110°
```

这个范围用于容纳实际视频中手臂姿态、MediaPipe 检测误差和人物身体倾斜带来的波动。

### 5.2 右 K `K_r`

右 K 对应 `tests/K_r.mp4`。

判断标准：

1. 左右上臂夹角绝对值满足 `65° <= arm_angle <= 110°`。
2. 左肩-左手腕线段 `(11,15)` 位于右躯干线 `(12,24)` 左侧。
3. 右肩-右手腕线段 `(12,16)` 位于右躯干线 `(12,24)` 左侧。
4. 两条肩-手腕线段的平均中点位于身体中心左侧。

第 4 条用于避免身体中间区域同时满足左右 K 的边界条件。

### 5.3 左 K `K_l`

左 K 对应 `tests/K_l.mp4`。

判断标准：

1. 左右上臂夹角绝对值满足 `65° <= arm_angle <= 110°`。
2. 左肩-左手腕线段 `(11,15)` 位于左躯干线 `(11,23)` 右侧。
3. 右肩-右手腕线段 `(12,16)` 位于左躯干线 `(11,23)` 右侧。
4. 两条肩-手腕线段的平均中点位于身体中心右侧。

## 6. K 动作 Confidence 计算

K 动作 confidence 不再只看上臂夹角，而是综合两部分：

```text
confidence = 0.6 * score_1 + 0.4 * score_2
```

### 6.1 score_1：双上臂夹角接近 90° 的程度

```text
score_1 = clamp(1 - abs(arm_angle - 90) / 25, 0, 1)
```

含义：

- `arm_angle` 越接近 `90°`，得分越高。
- 当 `arm_angle` 明显偏离 `90°` 时，得分降低。

### 6.2 score_2：左右手臂伸直程度

左臂伸直程度使用 `(11,13)` 和 `(13,15)` 的夹角衡量。

右臂伸直程度使用 `(12,14)` 和 `(14,16)` 的夹角衡量。

考虑到向量方向可能导致夹角接近 `0°` 或 `180°`，因此先转换为“偏离直线的程度”：

```text
left_straightness = min(left_elbow_angle, 180 - left_elbow_angle)
right_straightness = min(right_elbow_angle, 180 - right_elbow_angle)
```

然后计算左右手臂伸直得分：

```text
left_score = clamp(1 - left_straightness / 35, 0, 1)
right_score = clamp(1 - right_straightness / 35, 0, 1)
score_2 = (left_score + right_score) / 2
```

含义：

- 手臂越接近一条直线，`straightness` 越接近 0。
- `straightness` 越小，伸直得分越高。
- 左右两臂的伸直程度取平均作为 `score_2`。

## 7. 动作段合并规则

每一帧都会独立判断是否满足动作规则。连续满足同一动作规则的帧会合并为一个动作段。

默认最短动作段长度为：

```text
min_duration_sec = 0.20
min_frames = round(min_duration_sec * fps)
```

短于该长度的片段会被过滤，避免单帧抖动造成误识别。

## 8. 规则调整过程说明

### 8.1 初始方案

最初的规则主要根据左右肩部夹角判断 V / T / H 动作：

- 下 V 使用约 `30°-60°`。
- 大 T 使用约 `80°-100°`。
- 上 V 使用约 `110°-130°`。
- 上 H 使用接近 `180°`。

K 动作最初只通过左右上臂夹角约 `90°` 以及双臂位于身体某一侧来判断。

### 8.2 上 V 阈值调整

在测试中发现 `V_up` 容易漏检。原因是实际视频中的 MediaPipe 关键点会受到手臂抬起高度、身体倾斜和检测抖动影响，导致角度不稳定地落在原始 `110°-130°` 之外。

因此将 `V_up` 的范围放宽为：

```text
110°-165°
```

这样提高了 `V_up` 的召回率。代价是它与 `H_up` 在 `160°-165°` 有重叠，但该重叠区间被认为可以接受，因为它更符合实际视频中上 V 到上 H 之间的过渡状态。

### 8.3 K 左右识别反向问题

测试过程中发现 `tests/K_r.mp4` 被识别成了 `K_l`。说明仅使用肘点或简单身体中心投影来判断左右侧并不稳定。

后续将 K 的侧向判断改为更符合动作定义的线段关系：

- `K_r` 检查 `(11,15)` 和 `(12,16)` 是否在右躯干线 `(12,24)` 左侧。
- `K_l` 检查 `(11,15)` 和 `(12,16)` 是否在左躯干线 `(11,23)` 右侧。

同时增加肩-手腕线段平均中点相对身体中心的位置，避免左右 K 同时命中。

### 8.4 K 夹角范围调整

K 动作最初使用较严格的上臂夹角范围，例如 `80°-100°`。实际测试中发现识别率偏低，因此放宽为：

```text
65°-110°
```

这样可以容纳更多自然姿态变化，同时仍保留“接近 K 形”的基本限制。

### 8.5 K Confidence 调整

最初 K 的 confidence 主要由双上臂夹角决定。但实际动作中，仅靠上臂夹角不能很好地区分手臂是否形成清晰的 K 形。

因此增加了第二项评分：

- `score_1`：左右上臂夹角越接近 `90°` 越高。
- `score_2`：左右手臂越接近伸直越高。

最终使用：

```text
confidence = 0.6 * score_1 + 0.4 * score_2
```

这样可以让 K 的百分比同时反映 K 形夹角和手臂伸展质量。

## 9. 当前规则的特点与限制

当前规则的优点：

- 不需要提前注册模板。
- 对 6 个指定动作具有明确可解释的判断标准。
- confidence 可解释，便于根据测试视频继续调参。

当前规则的限制：

- 依赖 MediaPipe 关键点质量。如果肩、肘、腕、髋检测不稳定，规则结果会波动。
- `V_up` 与 `H_up` 存在有意保留的重叠区间。
- K 动作侧向判断基于当前测试视频的身体朝向和画面坐标，若拍摄方向变化较大，可能需要重新校准左右侧规则。

